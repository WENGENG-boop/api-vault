# Third-Party Notices

API Vault's bundled local-log parsers and model-pricing table include portions
adapted from `@vibe-cafe/vibe-usage` version 0.9.1, distributed under the MIT
License.

This attribution is a legal notice only. API Vault does not import that package
at runtime, connect to its services, sync data with it, or expose its branding
in the Local Tools product experience.

The local usage runtime is restricted to local files, local databases, and
loopback-only RPC. The upstream Cursor cloud-export parser is intentionally not
included.
